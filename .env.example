# WeighTogether Environment Configuration
#
# Copy this file to the appropriate environment file:
#   Development: cp .env.example .env.development
#   Production:  cp .env.example .env.production
#   Testing:     cp .env.example .env.test
#
# The app automatically loads the correct file based on NODE_ENV.

PORT=3000
# Options: development | production | test
NODE_ENV=development

# Database
# Development: Use Docker container (npm run db:start)
# Production: Use your production PostgreSQL instance
DATABASE_URL="postgresql://devuser:devpassword@localhost:5433/devdb?schema=public"

# JWT Secrets
# Development: These defaults are fine for local development
# Production: Generate secure secrets with: openssl rand -base64 48
JWT_ACCESS_SECRET="your-super-secret-access-token-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-token-key-change-in-production"

# Application URL (used for email links)
# Development: http://localhost:3000
# Production: https://yourdomain.com
APP_URL="http://localhost:3000"

# SMTP Configuration
# Development: Leave empty to log emails to console
# Production: Configure your SMTP server
SMTP_HOST=""
SMTP_PORT="587"
# true for port 465, false for other ports
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="WeighTogether <noreply@yourdomain.com>"
# Set to "false" only if mail server has certificate issues (not recommended for production)
# SMTP_REJECT_UNAUTHORIZED="true"

# PayPal Configuration
# Development: Use sandbox credentials from https://developer.paypal.com/
# Production: Use live credentials and set PAYPAL_USE_LIVE="true"
# See docs/PAYPAL_DEVELOPMENT_SETUP.md for complete setup instructions
PAYPAL_USE_LIVE="false"
PAYPAL_CLIENT_ID=""
PAYPAL_CLIENT_SECRET=""
PAYPAL_RETURN_URL="http://localhost:3000/donate/thank-you"
PAYPAL_CANCEL_URL="http://localhost:3000/donate"
PAYPAL_WEBHOOK_ID=""
PAYPAL_MONTHLY_PLAN_ID=""
PAYPAL_YEARLY_PLAN_ID=""

# GNews API (for news feature)
GNEWS_API_KEY=""

# Homepage Settings
HOME_NEWS_COUNT="6"

# Admin Settings
ADMIN_EMAIL=""
